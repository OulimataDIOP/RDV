<form method="post" action="{{ path('prestations_delete', {'id': prestation.id}) }}" onsubmit="return confirm('Etes-vous sûre de vouloir supprimer cette prestation ? ');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prestation.id) }}">
    <button class="btn">Supprimer</button>
</form>
